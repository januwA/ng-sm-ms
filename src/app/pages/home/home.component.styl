:host {
  position: absolute;
  padding: 1rem;
}

.image-card {
  float: left;
  min-width: 400px;
  max-width: 400px;
  margin: 1em;

  img {
    width: auto;
    height: auto;
    max-width: 100%;
    margin-left: 50%;
    transform: translateX(-50%);
  }
}