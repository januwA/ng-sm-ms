:host {
  height: 100vh;
  display: grid;
  place-items: center;

  .login-form {
    display: flex;
    flex-direction: column;
  }

  .register-button {
    text-align: end;
    font-size: 0.8em;
  }
}